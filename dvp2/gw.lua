if(get_window_name() == "gw") then
    undecorate_window()
    -- set_window_geometry(1080, 0, 280, 798 )
    set_window_geometry(1080, 0, 280, 798 )
    set_window_workspace(1)
    set_skip_tasklist(true)
    set_skip_pager(true)
end